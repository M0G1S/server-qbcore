# Network Endpoints (REQUIRED for txAdmin)
endpoint_add_tcp "0.0.0.0:30120"
endpoint_add_udp "0.0.0.0:30120"

# License Key (REQUIRED)
set sv_licenseKey "cfxk_13n93SKH36KEAvix0KuuY_mtlQZ"

# Server Info
set sv_hostname "My QBCore Server"  # Your server name
set sv_maxclients 32                # Max players
set steam_webApiKey ""              # Get from https://steamcommunity.com/dev/apikey (optional)
sets sv_projectName "Qwerty RP"
sets sv_projectDesc "QBCore Roleplay Server"

# Database (MySQL)
set mysql_connection_string "server=localhost;database=qbcore;userid=root;password="

# MUST LOAD FIRST
ensure oxmysql

# Core Framework
ensure qb-core

# Basic Resources (load after core)
ensure PolyZone          
ensure qb-target
ensure qb-cityhall
ensure qb-spawn
ensure qb-houses
ensure qb-vehicleshop
ensure qb-apartments

# Add other QBCore scripts here